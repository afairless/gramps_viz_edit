{
  "identifier": "default",
  "description": "Default capability granting dialog plugin permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}