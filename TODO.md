# Implementation Plan: Diff Tool Robustness to Dangling References

Source: `docs/research/robust-dangling-refs.md`

| # | Commit message | Logical unit | Key deliverables | Tests |
|---|---|---|---|---|
| 1 | `feat(typed-graph): add inferred-handle tracking to Graph` | Add `inferred_handles: HashSet<Handle>` to `Graph`, plus accessor methods | `crates/typed-graph/src/graph.rs`: add `use std::collections::HashSet`; add `inferred_handles` field to `Graph` struct; add `record_inferred_handle()`, `is_inferred_handle()`, `inferred_handle_count()` methods; initialize field in `Graph::new()` as empty `HashSet` | Unit: new graph has count 0, record one → count 1, `is_inferred_handle` returns true for recorded and false for unrecorded |
| 2 | `feat(gramps-reader): create placeholder nodes for dangling references` | In `build_edges()`, infer target `NodeKind` from edge type and create minimal placeholder nodes for missing targets | `crates/gramps-reader/src/xml/parse.rs`: add `target_kind_for_edge()` mapping (exhaustive match on `PendingEdge` + `SimpleEdgeKind`); add `placeholder_node()` factory; modify `build_edges()` to check target existence and create placeholders; handle kind conflicts with `log::warn!` | Unit: parse XML with dangling `noteref` → placeholder Note node, edge exists, `is_inferred_handle()` true. Dangling `citationref` → placeholder Citation. All 10 primary target kinds (Person, Family, Event, Place, Source, Citation, Repository, Media, Note, Tag) exercised. Intact graph → no placeholders. Kind-conflict scenario → first edge wins, warning logged. |
| 3 | `feat(gramps-reader): downgrade validation errors to warnings` | In `parse_graph()`, call `graph.validate()` directly and log errors as `warn!` instead of returning `Err` | `crates/gramps-reader/src/xml/parse.rs`: change `parse_graph` to call `parser.graph.validate(schema)`, iterate errors with `log::warn!`, return `Ok(parser.into_graph())` unconditionally | Integration: parse graph with structural validation errors → succeeds with logged warnings (captured via `testing_logger` or equivalent) |
| 4 | `feat(diff): add dangling reference counts to diff report` | Add `dangling_count_a` / `dangling_count_b` to `DiffSummary`, populate in `run_diff`, include in text/JSON output | `crates/diff/src/report.rs`: two new `usize` fields on `DiffSummary` (default 0). `crates/diff/src/lib.rs`: in `run_diff`, assign `graph_a.inferred_handle_count()` and `graph_b.inferred_handle_count()`. `crates/diff/src/output.rs`: include counts in text and JSON output. | Unit: `DiffSummary` defaults have 0 counts, serde round-trip. Integration: diff two files where one has dangling ref → counts 1 and 0. Diff with different dangling handles per file → counts 1 and 1. Diff where same handle is dangling in both → counts 1 and 1, items match as `Same`. |
